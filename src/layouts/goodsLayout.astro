---
const { frontmatter } = Astro.props;
import "../style/style.scss";
---

<body>
    <main class="single">
        <div class="flex">
            <div class="images">
                <img
                    class="thumb"
                    src={frontmatter.thumbnail}
                    alt={frontmatter.title}
                />
                <img
                    class="mini"
                    src={frontmatter.subpic1}
                    alt={frontmatter.title}
                />
                <img
                    class="mini"
                    src={frontmatter.subpic2}
                    alt={frontmatter.title}
                />
                <img
                    class="mini"
                    src={frontmatter.subpic3}
                    alt={frontmatter.title}
                />
            </div>
            <div class="contents">
                <h2>{frontmatter.title}</h2>
                <h3>商品説明</h3>
                <p class="price">{frontmatter.price}</p>
                <p class="text">{frontmatter.text}</p>
            </div>
        </div>
    </main>

    <script src="../js/main.js"></script>
</body>
<slot />
